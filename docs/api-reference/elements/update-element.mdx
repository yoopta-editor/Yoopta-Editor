---
title: updateElement
description: Update properties of existing elements
---

## Overview

The `updateElement` method modifies properties of an existing element within a block.

## Signature

```typescript
editor.updateElement(options: UpdateElementOptions): void
```

## Parameters

<ParamField path="options" type="UpdateElementOptions" required>
  Configuration object for updating the element

  <Expandable title="properties">
    <ParamField path="blockId" type="string" required>
      ID of the block containing the element
    </ParamField>

    <ParamField path="type" type="string" required>
      Type of element to update
    </ParamField>

    <ParamField path="props" type="Record<string, unknown>">
      Properties to update on the element. Will be merged with existing props.
    </ParamField>

    <ParamField path="path" type="ElementPath">
      Path to the element:
      - `'first'` - First element of the specified type
      - `'last'` - Last element of the specified type
      - `'selection'` - Element at current selection
      - `number[]` - Specific Slate path
    </ParamField>

    <ParamField path="match" type="(element: SlateElement) => boolean">
      Custom matcher function to find the element. If provided, takes precedence over `type` and `path`.
    </ParamField>

  </Expandable>
</ParamField>

## Examples

### Basic Usage

```typescript
import { useYooptaEditor } from '@yoopta/editor';

function AccordionItem({ blockId, itemPath }: Props) {
  const editor = useYooptaEditor();

  const toggleExpanded = () => {
    const item = editor.getElement({
      blockId,
      type: 'accordion-list-item',
      path: itemPath,
    });

    editor.updateElement({
      blockId,
      type: 'accordion-list-item',
      props: { isExpanded: !item?.props?.isExpanded },
      path: itemPath,
    });
  };

  return <button onClick={toggleExpanded}>Toggle</button>;
}
```

### Update with Matcher

```typescript
// Update active tab
editor.updateElement({
  blockId: 'tabs-1',
  type: 'tabs-item-heading',
  props: { active: false },
  match: (el) => el.type === 'tabs-item-heading' && el.props?.active === true,
});
```

### Update by Path

```typescript
// Update first item
editor.updateElement({
  blockId: 'accordion-1',
  type: 'accordion-list-item',
  props: { isExpanded: true },
  path: 'first',
});

// Update last item
editor.updateElement({
  blockId: 'accordion-1',
  type: 'accordion-list-item',
  props: { isExpanded: false },
  path: 'last',
});

// Update at specific path
editor.updateElement({
  blockId: 'table-1',
  type: 'table-data-cell',
  props: { width: 300, asHeader: true },
  path: [0, 0, 0],
});
```

## Use Cases

### Toggle Accordion Item

```typescript
const toggleAccordionItem = (blockId: string, itemPath: number[]) => {
  const item = editor.getElement({
    blockId,
    type: 'accordion-list-item',
    path: itemPath,
  });

  if (item) {
    editor.updateElement({
      blockId,
      type: 'accordion-list-item',
      props: { isExpanded: !item.props?.isExpanded },
      path: itemPath,
    });
  }
};
```

### Set Active Tab

```typescript
const setActiveTab = (blockId: string, tabId: string) => {
  // Deactivate all tabs
  const allTabs = editor.getElements({
    blockId,
    type: 'tabs-item-heading',
  });

  allTabs.forEach((tab, index) => {
    editor.updateElement({
      blockId,
      type: 'tabs-item-heading',
      props: { active: tab.id === tabId },
      path: [0, 0, index],
    });
  });
};
```

### Update Table Cell

```typescript
const updateCellProperties = (
  blockId: string,
  cellPath: number[],
  properties: { width?: number; asHeader?: boolean; align?: string },
) => {
  editor.updateElement({
    blockId,
    type: 'table-data-cell',
    props: properties,
    path: cellPath,
  });
};
```

### Update Step Completion

```typescript
const markStepComplete = (blockId: string, stepIndex: number) => {
  editor.updateElement({
    blockId,
    type: 'step-list-item',
    props: { isCompleted: true },
    path: [0, 0, stepIndex],
  });
};
```

## Advanced Usage

### Update Multiple Elements

```typescript
const expandAllAccordionItems = (blockId: string) => {
  const items = editor.getElements({
    blockId,
    type: 'accordion-list-item',
  });

  items.forEach((item, index) => {
    editor.updateElement({
      blockId,
      type: 'accordion-list-item',
      props: { isExpanded: true },
      path: [0, 0, index],
    });
  });
};
```

### Conditional Update

```typescript
const updateIfNotEmpty = (blockId: string, itemPath: number[]) => {
  const isEmpty = editor.isElementEmpty({
    blockId,
    type: 'accordion-list-item-content',
    path: [...itemPath, 1],
  });

  if (!isEmpty) {
    editor.updateElement({
      blockId,
      type: 'accordion-list-item',
      props: { hasContent: true },
      path: itemPath,
    });
  }
};
```

## Notes

<Note>
  Properties are merged with existing props. To remove a property, set it to `undefined` or `null`.
</Note>

<Warning>
  If the element is not found (no match for type, path, or matcher), the operation will silently
  fail. Always check if the element exists before updating if you're unsure.
</Warning>

<Info>
  When using a custom `match` function, it takes precedence over `type` and `path`. The function
  receives each element and should return `true` for the element you want to update.
</Info>

## Type Definition

```typescript
type UpdateElementOptions = {
  blockId: string;
  type: string;
  props?: Record<string, unknown>;
  path?: ElementPath;
  match?: (element: SlateElement) => boolean;
};

type ElementPath = number[] | 'selection' | 'first' | 'last';
```

## Related Methods

<CardGroup cols={2}>
  <Card title="getElement" icon="search" href="/api-reference/elements/get-element">
    Get element before updating
  </Card>
  <Card title="insertElement" icon="plus" href="/api-reference/elements/insert-element">
    Create new elements
  </Card>
  <Card title="deleteElement" icon="trash" href="/api-reference/elements/delete-element">
    Remove elements
  </Card>
  <Card title="isElementEmpty" icon="check-circle" href="/api-reference/elements/is-element-empty">
    Check element state before update
  </Card>
</CardGroup>
